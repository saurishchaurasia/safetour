# Mobile App Placeholder

The frontend is mobile-first and works as a responsive web app. This folder is reserved for a future React Native or Capacitor wrapper.

Recommended next step:

```powershell
npm create capacitor@latest mobile -- --web-dir ../frontend/dist
```

Mobile-specific features to add:

- Native push notifications
- Background location tracking
- Voice-activated SOS
- Offline emergency mode with queued SMS
- Camera-based facial verification
